<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.InventoryCheck">

    <uses-permission android:name="android.permission.CAMERA" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.InventoryCheck">

        <activity android:name="com.example.InventoryCheck.AddEditBarcodesActivity"
            android:parentActivityName="com.example.InventoryCheck.MainActivity"/>


        <activity android:name="com.example.InventoryCheck.MainActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.InventoryCheck.BarcodeCaptureActivity"
            android:label="Read Barcode"/>
    </application>

</manifest>